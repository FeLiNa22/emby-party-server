{
  "name": "emby-party-server",
  "version": "1.0.0",
  "description": "Allow multiple user's who have access to an emby server to view the same film at the same time",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon --watch './src/**/*.ts' --exec npm start",
    "build": "tsc",
    "start": "npm run build && node ./build/Server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.17",
    "@types/randomstring": "^1.1.6",
    "@types/socket.io": "^2.1.12",
    "express": "^4.17.1",
    "randomstring": "^1.1.5",
    "socket.io": "^3.0.4",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
